#include <fstream>
#include <math.h>
#include <vector>
#include <iostream> //remove after debugging

#DEFINE PREC 5

typedef unsigned int u32;
typedef unsigned char u8;
typedef double db;

db setP(db num) {
  num = (int)(num * (pow(10,PREC)));
  db res = (db)(n / pow(10,PREC));
  return res;
}

u32 iterate(const std::complex<db>& zorc) {
  
}

int main() {
  //pass
}
